import React from 'react';

const Header = (props) => (<h1>{this.props.header}</h1>)

const Code = (props) => (<p>Header</p>)

const Paragraph = (props) => (<p className={this.props.type}>Header{this.props.text}</p>)

export default Paragraph;